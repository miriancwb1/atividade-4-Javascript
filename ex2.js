let somador = 0;
let contador = 1; // Inicia a variável contadora em 1
while (contador <= 100) {
    somador += contador; // Adiciona o contador ao somador
    contador++; // Incrementa o contador
}
console.log("Soma de 1 a 100: " + somador);

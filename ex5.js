let contador = 1; // Inicia a variável em 1
while (contador <= 20) {
    if (contador % 2 !== 0) { // Verifica se o número é ímpar
        console.log(contador); // Exibe o número ímpar
    }
    contador++; // Incrementa o contador
}

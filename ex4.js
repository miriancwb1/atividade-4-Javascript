let contador = 1; // Inicia a variável em 1
while (contador <= 20) {
    if (contador % 2 === 0) { // Verifica se o número é par
        console.log(contador); // Exibe o número par
    }
    contador++; // Incrementa o contador
}
